- Points 1,2,3 and 4 can be done with API calls in a Python scrypt (test.py). The script uses pagination, as our domain contains more than 200 users.
- Bonus 1 - This can be done by scheduled task on a server - Windows Task scheduler or Cron.
  This can also be set with Okta Workflows.
- Bonus 2- The scripts exports a file named App_Removed_users.csv, which contains the User ID, Team and last logon date
